name: Build & Push Docker Image (api)

on:
  push:
    branches:
      - master

env:
  IMAGE_NAME: daffa09/emobo-ecommerce-api
  IMAGE_VERSION: 1.0.1

jobs:
  build-and-push:
    runs-on: ubuntu-latest

    steps:
      - name: 🧩 Checkout source
        uses: actions/checkout@v4

      - name: 🔧 Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: 🔑 Login to Docker Hub
        uses: docker/login-action@v3
        with:
          username: ${{ secrets.DOCKERHUB_USERNAME }}
          password: ${{ secrets.DOCKERHUB_TOKEN }}

      - name: 🏗️ Build and push Docker image
        uses: docker/build-push-action@v6
        with:
          context: .
          file: Dockerfile
          push: true
          build-args: |
            ENV_FILE_CONTENT=${{ secrets.ENV_FILE_CONTENT }}
          tags: |
            ${{ env.IMAGE_NAME }}:${{ env.IMAGE_VERSION }}
            ${{ env.IMAGE_NAME }}:latest

      - name: ✅ Summary
        run: |
          echo "✅ Build and push success!"
          echo "📦 Image: ${{ env.IMAGE_NAME }}:${{ env.IMAGE_VERSION }}"

      # - name: 🚀 Deploy API Zero Downtime
      #   uses: appleboy/ssh-action@v1.0.3
      #   with:
      #     host: ${{ secrets.SERVER_HOST }}
      #     username: ${{ secrets.SERVER_USER }}
      #     key: ${{ secrets.SERVER_KEY }}
      #     script: |
      #       cd ~/skripsi
      #       docker compose pull api
      #       docker compose up -d --scale api=2 --no-recreate api
      #       echo "⏳ Waiting for API to boot..."
      #       sleep 15
      #       docker compose up -d --scale api=1 api
      #       docker compose exec -T gateway nginx -s reload
      #       docker image prune -f
      #       echo "🕷️ Running Manual Crawling Script..."
      #       docker compose exec -T api node dist/scripts/manual-crawling.js
      #       docker image prune -f